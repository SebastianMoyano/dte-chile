fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.3.0
lxml>=5.2.0
signxml>=3.2.0
cryptography>=42.0.0
httpx>=0.27.0
python-jose[cryptography]>=3.3.0
reportlab>=4.2.0
pdf417gen>=0.7.0
jinja2>=3.1.4
aiofiles>=23.2.0
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
mcp>=1.2.0
# Base de datos de zonas horarias. En Linux/macOS `zoneinfo` usa la del sistema, pero
# Windows NO trae ninguna: sin esto, el programador del RVD (que fija el día en la hora de
# Chile, no la del servidor) falla al arrancar allí.
tzdata>=2024.1

# Postulación a certificación de BOLETAS: el portal certBolElectDteInternet es una app GWT
# (no scriptable por httpx). core/postulacion.py la maneja con playwright. Import perezoso:
# el resto del motor NO lo necesita. Instalar solo si vas a postular boletas:
#   .venv/bin/pip install playwright && .venv/bin/python -m playwright install chromium
# playwright>=1.40
